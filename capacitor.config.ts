import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.federicoscutariu.spaggiari2',
  appName: 'Spaggiari 2',
  webDir: 'public',
  server: {
    androidScheme: 'https'
  }
};

export default config;
